<UserControl x:Class="FoundOps.Common.Silverlight.UI.Controls.AddEditDelete.AddToDeleteFrom" x:Name="myAddToDeleteFrom"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns:AddEditDelete="clr-namespace:FoundOps.Common.Silverlight.UI.Controls.AddEditDelete" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:MarkupExtensions="clr-namespace:FoundOps.Common.Silverlight.UI.Tools.MarkupExtensions" xmlns:Behaviors="clr-namespace:FoundOps.Common.Silverlight.UI.Interactivity.Behaviors" mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="AddMenuTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Text="Location" />
                <telerik:RadComboBox x:Name="RadComboBox" Grid.Row="1" MinWidth="120" StaysOpenOnEdit="True" IsEditable="True" 
                                     EditableTemplate="{StaticResource SL5BugWorkaround}"
                                     ItemsSource="{Binding Provider.ExistingItemsSource, RelativeSource={RelativeSource AncestorType=AddEditDelete:AddToDeleteFrom}}" 
                                     DisplayMemberPath="{Binding Provider.MemberPath, RelativeSource={RelativeSource AncestorType=AddEditDelete:AddToDeleteFrom}}"
                                     SelectedValue="{Binding SelectedExistingItem, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=AddEditDelete:AddToDeleteFrom}}"
                                     Text="{Binding ExistingItemsComboBoxText, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=AddEditDelete:AddToDeleteFrom}}"
                                     TextSearchMode="StartsWith" />
                <StackPanel Orientation="Horizontal" Margin="5,2,0,0">
                    <Button Grid.Row="2" Content="Add New" Margin="0,2,0,0" Click="AddNewButtonClick" IsEnabled="{Binding SelectedValue, ElementName=RadComboBox, Converter={StaticResource NotExistsBoolConverter}}"/>
                    <Button Grid.Row="2" Content="Add Existing" Margin="3,2,0,0" Click="AddExistingButtonClick" IsEnabled="{Binding SelectedValue, ElementName=RadComboBox, Converter={StaticResource ExistsBoolConverter}}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <AddEditDelete:AddDelete x:Name="AddDelete" Grid.Column="1" Add="AddDelete_OnAdd" Delete="AddDelete_OnDelete" AddDeleteMode="AddDelete" AddMenuItemCustomTemplate="{StaticResource AddMenuTemplate}" />
    </Grid>
</UserControl>
