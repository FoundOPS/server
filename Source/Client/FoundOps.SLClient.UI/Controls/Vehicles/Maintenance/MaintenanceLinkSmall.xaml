<UserControl x:Class="FoundOps.Framework.Views.Controls.Vehicles.Maintenance.MaintenanceLinkSmall"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ViewModelLocator="clr-namespace:MEFedMVVM.ViewModelLocator;assembly=MEFedMVVM.SL"
     mc:Ignorable="d" d:DesignWidth="300"
    ViewModelLocator:ViewModelLocator.SharedViewModel="VehicleMaintenanceVM">
    <Grid x:Name="LayoutRoot" Margin="5">
        <ListBox ItemsSource="{Binding SelectedEntity.LineItems}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="$" />
                        <TextBlock Text="{Binding Cost}" />
                        <TextBlock Text=" " />
                        <TextBlock Text="{Binding Type}" />
                        <TextBlock Text=" " />
                        <TextBlock Text="{Binding Details}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
