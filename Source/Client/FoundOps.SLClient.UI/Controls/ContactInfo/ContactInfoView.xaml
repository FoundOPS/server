<UserControl x:Class="FoundOps.SLClient.UI.Controls.ContactInfo.ContactInfoView" x:Name="myContactInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Converters="clr-namespace:FoundOps.SLClient.Data.Converters;assembly=FoundOps.SLClient.Data"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="30">
    <UserControl.Resources>
        <Converters:OrderByTypeLabelConverter x:Key="OrderByTypeLabelConverter" />
        <DataTemplate x:Key="ContactInfoTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Type}"/>
                <TextBlock Text=": "/>
                <TextBlock Text="{Binding Label}"/>
                <TextBlock Text=" "/>
                <TextBlock Text="{Binding Data}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" DataContext="{Binding ContactInfoVM, ElementName=myContactInfoView}">
        <StackPanel HorizontalAlignment="Center">
            <ItemsControl ItemsSource="{Binding ContactInfoSet, Converter={StaticResource OrderByTypeLabelConverter}}"
				    ItemTemplate="{StaticResource ContactInfoTemplate}" />
        </StackPanel>
    </Grid>
</UserControl>
